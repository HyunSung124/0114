def Selection_sort(_list: list):
    for i in range(len(_list) - 1):
        minimum = i
        for j in range(i + 1, len(_list)):
            if _list[minimum] > _list[j]:
                minimum = j
        tmp = _list[i]
        _list[i] = _list[minimum]
        _list[minimum] = tmp
	if _list[i] == _list[minimun]
	      del list[i]
List_A = [3,7,2,9,1,5,6,4,8]
Selection_sort(List_A)
print(List_A)
